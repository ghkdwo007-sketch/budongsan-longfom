# 학습 누적 노트

비블의 실제 작업물과 엔진 출력을 대조해 알아낸 것들. **회차를 거듭하며 여기에 쌓는다.**

> **이 파일이 존재하는 이유:** Claude Code 의 메모리는 작업 PC 의
> `~/.claude/projects/<프로젝트>/memory/` 에 저장돼서 **외장 SSD 를 따라오지 않는다.**
> 다른 환경에서 이 드라이브만 열어도 학습 내용이 이어지도록 같은 내용을 여기에 둔다.
> 새로 배운 게 생기면 **메모리와 이 파일 양쪽에** 남길 것.

상세 근거와 수치는 `profiles/budongsan-longfom/README.md` 에 있다. 여기는 요약과 색인이다.

---

## 1. 편집 학습 루프 — 어떻게 재는가

비블의 목표는 **엔진 결과물과 자기가 수정한 결과물의 차이를 좁히는 것**이다.

```bash
python engine/mcp_cut_diff.py "<수정본 시퀀스 이름>" \
  "output/_prev_260728_부동산/<base>_cut.xml" "output/_v207_260730/<base>_cut.xml"
```

프리미어를 열어 둔 채 MCP 로 직접 읽는다 — 렌더도 EDL 재내보내기도 필요 없다.
**기준선 2개를 함께 넘길 것**(285컷 납품본 / 207컷 현재 엔진). 하나만 보면 이미 고친 것까지
두 번 보정하게 된다. 기준선 폴더(`output/_prev_260728_부동산/`, `output/_v207_260730/`)를
지우지 말 것 — `output/` 은 gitignore 라 **git 으로는 안 따라오고 드라이브에만 있다.**

## 2. 컷 — 엔진 몫은 전체의 1/4 이다

| | 컷 | 유지 | 원본대비 |
|---|---|---|---|
| 비블 완성본 | 174 | 13:50 | **46.0%** |
| 엔진 285컷(납품) | 285 | 26:14 | 87.2% |
| 엔진 207컷(현재) | 207 | 27:50 | 92.5% |

- **'과잉 제거' 가정은 틀렸다.** 우리가 버린 3:52 중 비블이 되살린 건 **3.1초**뿐이다.
  무음 가드로 2.3분 덜 자른 건 얻은 게 2.3초고, 그 2.3분은 결국 다 잘렸다.
  가드는 접합부 음질 때문에 유지하되 **제거량은 되찾는 방향**(`CUT_GATE_MARGIN` 완화).
- **컷 경계 델타 중앙값 0.000s** = 비블이 우리 경계를 그대로 썼다. 패딩은 손대지 말 것.
- 차이 12~14분은 전부 **내용 컷**이다. 러프컷을 '거의 완성'으로 취급하지 말 것.
  삭제 기준은 프로파일 README 의 '내용 컷 기준' 표 참고(잡담·사담·스튜디오 농담·나열형 TMI).

## 3. 자막 — 문서에 적혀 있던 규칙 2개가 반대였다

```bash
python engine/sub_diff.py "<프로젝트.prproj>" "output/<base>_cut.srt"
```

- **온점·쉼표는 지운다**(`SUB_STRIP_PUNCT="all"`). `? !` 와 `... …`, 숫자 안의 점·쉼표는 남긴다.
  > **[철회]** 여기 한때 "지우면 안 된다"고 썼다가 되돌렸다. 근거로 삼은 프리미어 캡션
  > (온점 225·쉼표 65)이 **26.23분** 타임라인에 있었는데 그건 우리 285컷 납품본 길이(26.24분)고,
  > 비블 최종본은 **13.84분**이다 — 완성본이 아니라 납품 직후 중간 상태였다.
  > **캡션을 근거로 쓸 때는 얹힌 시퀀스 길이가 최종본과 맞는지 먼저 확인할 것.**
  > 텍스트가 99.90% 일치한다고 그 트랙이 최종 상태인 건 아니다.
- **한 줄 25자.** 20으로 낮췄다가 되돌렸다 — 캡션 중앙이 15자라 낮췄는데 화면에서 짧았다.
  ①과 같은 뿌리의 오류다(그 26.23분 캡션에서 나온 결론). **문장부호도 길이도 둘 다 철회.**
  자막 길이는 `subtitle_polish` 가 아니라 상류 `make_subtitles.regroup(max_chars=)` 에서
  정해진다 — 바꾸려면 컷 단계를 다시 돌려야 한다(전사 캐시가 있으면 재전사는 없다).
- 줄 경계 자체는 옳았다(이동 5건/633줄). 어미 사전 분할은 방향이 맞다.
- **단독 리액션('네')은 본 자막에서 뺀다** — C1·C2 는 "아~", "진짜~?" 같은
  예능형 재미 자막 전용 레이어다.
- 사투리 어감은 표준어로 펴지 말 것(`갱상도`→`경상도`는 고치되 `아닙니까`→`아닙니꺼`는 살림).
- **캡션은 프리미어 API 로 못 읽는다** — MCP `read_sequence_captions` 는 항상 `trackCount:0`.
  `engine/prproj_captions.py` 가 `.prproj`(gzip XML)를 직접 판다.

## 4. 색보정 — "색보정 해줘" 하면 이 레시피로

```bash
python engine/apply_grade_still.py "<이미지>"            # 확정 레시피
python engine/apply_grade_still.py "<이미지>" --compare  # 원본과 나란히
```

**제1원칙(비블):** 색을 입히는 게 아니라 **지배적인 캐스트를 걷어내 중성으로 되돌린다.**
붉은기가 강하면 붉은 기를, 녹색기가 강하면 녹색 기를, 푸른기가 강하면 푸른 기를 걷어낸다.
→ **방향은 소스마다 다르다.** 지난 회차 값을 복사하지 말고 **재서 반대로 민다**
(`whitebalance()` 가 그 구현 — 벽처럼 원래 중성인 면 기준).

| | 단계 | 값 |
|---|---|---|
| ① | 휠 루마 | 0.18 (섀도우 0.032 / 미드 0.846 / 하이 0.940) |
| ② | 휠 틴트 | 주황 +35.3°, 강도 0.27 |
| ③ | 중성면 WB | 벽 기준 채널 균등화 |
| ④ | 흰색 | +12 |
| ⑤ | 크리에이티브 채도 | UI 98 |
| ⑥ | 블루 틴트 | +5pt |
| ⑦ | 피부 채도 | +40 (색상 0~45°·채도 0.12~0.65·명도>0.25) |

**③~⑤ 중성화 → ⑥~⑦ 스타일. 순서를 바꾸지 말 것.**
결과 보고에 **밝기·화면채도·피부채도·중성면 R−B·날아간 픽셀%** 를 항상 붙인다.
밝기·틴트를 올린 뒤에는 **소프트 니를 반드시 다시** 걸 것(안 하면 7% 가 날아간다).
피부 +40 은 상한선에 가깝다 — 더 올리면 과포화를 경고할 것.

원본 Lumetri 값은 `engine/read_grade.py` 로 읽는다. **MCP `list_clip_effects` 는 쓰지 말 것**
— trackIndex/clipIndex 를 무시하고 항상 첫 클립만 준다.

## 5. 프리미어 MCP

`engine/premiere_mcp.py` 가 stdio 로 직접 붙는다 — 세션에 `mcp__premiere-pro__*` 툴이
안 올라와 있어도 쓸 수 있다. 서버 위치는 환경변수 → `~/.claude.json` → 흔한 폴더 순으로
자동 탐색한다. 못 찾으면 `PREMIERE_MCP_SERVER` 로 알려주면 된다.

전제: 프리미어 실행 중 + **CEP 브리지 패널이 열려 있고 Start Bridge 를 눌러둔 상태**.
ExtendScript 는 마지막 식이 아니라 **IIFE 의 return** 만 받는다.
파라미터를 100개 넘게 도는 스크립트는 브리지가 타임아웃 난다 — 30개 이하로 끊을 것.
