# 부동산롱폼 — 편집 프로파일

비블 부동산 롱폼(대표님 & 미빅님 Q&A 등) 전용 워크플로. 2캠 교차편집 + 컷편집 + 자막.
회차마다 이 문서 순서대로 돌리면 된다. 새로 알게 된 건 여기와 `glossary.txt` 에 계속 쌓는다.

**최초 확립: 2026-07-29** — 2캠 Q&A 회차로 전 과정을 돌려보며 정리했다.

> 폴더명은 `budongsan-longfom` 인데 명령은 `--profile 부동산롱폼` 이다. 오타가 아니라
> `config.json` 의 `"_프로파일"` 별칭으로 찾는다. 한글 폴더명은 맥(NFD)과 윈도우(NFC)가
> 다르게 저장해서 외장 SSD 를 오가면 중복 커밋된다 — 그래서 폴더만 ASCII 로 뒀다.

## 한 줄로 돌리기

```bash
python engine/run_episode.py \
  --cam1 "D:/.../cam01/DJI_xxxx.MP4" \
  --cam2 "D:/.../cam02/DJI_yyyy.MP4" \
  --profile 부동산롱폼
```

0~6단계(TC0 사본 → 싱크 → 컷 → 자막 → 오디오 → EDL → 검증)를 순서대로 실행한다.
`--offset` 을 주면 싱크 측정을 건너뛰고, `--skip-remux` 로 TC0 사본 생성을 건너뛴다.

`--segments output/<base>_segments.json` 을 주면 **7단계(카테고리 분류 + 축약본)**까지 만든다.
구간 판단은 전사본을 읽어야 하는 일이라 자동화하지 않았다 — Claude Code 에
"카테고리 나누고 N분 하이라이트 만들어줘" 라고 하면 JSON 을 만들고 실행까지 해준다.

납품 전 검증만 따로:
```bash
python engine/verify_episode.py "<cam01 원본.MP4>" --cam2
```

아래는 각 단계에서 **왜 그렇게 하는지** — 순서를 바꾸거나 생략하면 깨지는 이유들이다.

---

## 작업 환경

| | |
|---|---|
| OS | Windows 11 · Python 3.11 · ffmpeg 8.1.2 |
| GPU | RTX 4060 8GB (faster-whisper CUDA) |
| 편집 | **Adobe Premiere Pro 26.3.0** |
| 소스 | DJI 카메라 2대, 4K HEVC 3840×2160 @ 29.97fps, 약 30분 / 11GB |

## [핵심] Premiere 26 은 FCP7 XML 을 못 읽는다

26.3 설치 폴더에 xmeml 임포터가 **없다**(AAF·OMF·EDL 만 남음). 가져오기 형식 목록에도 없다.
작은 XML(5~10컷)은 우연히 통과하지만 20컷만 넘어도 "프로젝트가 손상되어 열 수 없습니다"로 실패한다.

→ **핸드오프는 EDL 로 한다.** `_cut.xml` / `_mcam.xml` 은 Premiere 25 이하·타 NLE 용으로만 남겨둔다.

---

## 순서

### 0) 소스 준비 — TC0 사본 (필수)

EDL 은 파일 경로가 아니라 **릴 이름 + 타임코드**로 미디어를 찾는다. 그래서
**파일의 TC 와 EDL 의 TC 기준이 반드시 같아야 한다.** DJI 파일은 카메라 시계가 박혀 있고
(예: cam01 `23:50:03;28`, cam02 `20:21:28;04`) 두 캠이 서로 다르다.

두 캠 **모두** TC 0 사본을 만든다. 재인코딩 없음(컨테이너만 다시 씀) → 화질 손실 0, 1~3분.

```bash
ffmpeg -i cam01/원본.MP4 -map 0:v:0 -map 0:a:0 -c copy -timecode 00:00:00:00 cam01/cam01_TC0.MP4
ffmpeg -i cam02/원본.MP4 -map 0:v:0 -map 0:a:0 -c copy -timecode 00:00:00:00 cam02/cam02_TC0.MP4
```

`-map 0:v:0 -map 0:a:0` 는 DJI 가 붙이는 mjpeg 썸네일·데이터 스트림을 떼어낸다(파일도 가벼워짐).

> **실패 사례**: cam02 만 실제 TC(20:21…)로 EDL 을 만들었더니, 프리미어가 미디어 TC 를 0 으로 잡아
> 20시간대 지점을 맞추려고 **23시간짜리 유령 클립**을 만들었다. 컷 위치·길이가 전부 어긋나고 렉도 심해진다.
> 프리미어의 `클립 수정 > 시간 코드` 는 **오프라인 클립에서 비활성**이라 이걸로는 해결이 안 된다.

### 1) 싱크 오프셋

```bash
python engine/sync_2cam.py "cam01/cam01_TC0.MP4" "cam02/cam02_TC0.MP4"
```

파일명 타임스탬프는 믿지 말 것 (실측에서 28초 차이로 표시됐지만 실제는 1.04초였다).
`engine/` 밖에서 구간별 재측정으로 드리프트도 확인하면 좋다 — 실측 30분 누적 18.8ms(0.56프레임)로 보정 불필요.

### 2) 컷편집

```bash
python engine/auto_cut.py "cam01/cam01_TC0.MP4" --preset 표준 --profile 부동산롱폼
```

- 프리셋은 **표준**. `공격`은 추임새를 지워 이 채널의 말맛(만담·반복 개그)을 죽인다.
- `--profile` 이 자막 25자 등 이 코너 설정을 얹는다.

#### [중요] 컷은 오디오가 거의 0인 자리에서만 낸다 (2026-07-30)

비블 피드백: **말이 채 끝나기 전에 끊겨서 다음 컷과의 연결이 부자연스러웠다.**
원인이 두 군데였고, 설정과 코드 양쪽을 고쳤다.

**① 무음 컷 경계** — 프로파일 값으로 해결.

| | 전 | 후 | 이유 |
|---|---|---|---|
| `NOISE_DB` | -45 | **-50** | 더 조용해야 무음으로 인정 = "거의 0" |
| `PAD_LEAD` | 0.15 | **0.30** | 말 끝난 뒤 여유를 두 배로 (어미 울림 보존) |
| `PAD_TAIL` | 0.10 | **0.12** | |
| `MIN_SILENCE` | 0.45 | **0.70** | 패딩을 키운 만큼 함께 올려야 한다 ↓ |
| `MIN_REMOVE` | (없음) | **0.20** | |

> **`PAD_LEAD+PAD_TAIL` 과 `MIN_SILENCE` 는 같이 움직여야 한다.** 패딩 합이 무음 길이에
> 가까워지면 '지우는 건 50ms인데 접합부는 생기는' 미세컷이 대량으로 생긴다. 새 값은
> 0.42 vs 0.70 이라 컷당 최소 0.28초를 지운다. `MIN_REMOVE` 는 이 여유가 그보다도
> 작아지는 자리를 아예 컷하지 않게 하는 안전망이다(패딩만 키우면 무음 흡수로 자동
> 병합되지만, 경계값 부근을 확실히 막는다).

**② 말 도중을 잘라내는 제거** — 설정으로 표현이 안 돼 `engine/cut_gate.py` 를 새로 만들었다.

더듬/중복·NG·망설임·숨소리는 전사본의 단어 타임스탬프로 구간을 잡는데, whisper 의 단어
경계는 ±100~200ms 흔들린다. **전사상 '단어 사이 빈틈'인 자리가 실제로는 아직 말이 울리는
중**이라 거기서 이어붙으면 음절이 잘린다. `WORD_SNAP` 은 경계가 단어 구간을 '엄격히 관통'할
때만 고치므로 이걸 못 잡는다 — 타임스탬프 자체가 틀린 것이기 때문이다.

가드는 이어붙을 두 지점의 실제 RMS 를 재서 ① 조용하면 통과 ② 아니면 근처에서 조용한 자리를
찾아 옮기고 ③ 그래도 없으면 **그 제거를 포기한다**(반복 구간에서 소리가 겹쳐 끊을 자리가
없으면 놔두는 게 낫다는 편집 방향). 기준은 `노이즈플로어 + CUT_GATE_MARGIN(6dB)` 로,
회차마다 녹음 레벨이 달라도 따라간다. 포기한 구간은 리포트의 `가드보류` 항목에 남는다.

> **가드는 두 번 돌아야 한다.** 제거 목록만 검사하면 부족하다 — 그 뒤 `WORD_SNAP` 이
> 경계를 단어 끝으로 되돌리고, 무음 컷 경계는 애초에 첫 패스를 거치지 않는다.
> 그래서 최종 컷 목록에 한 번 더 패스를 돌린다(`gate_keeps`). 여기서 '포기'는
> **두 컷을 합쳐 그 자리를 안 끊는 것**이다.
>
> **판정은 프레임 격자에서 해야 한다.** 자막 드리프트 때와 같은 함정이다. 실수 초로
> '조용하다'고 판정해도 XML/EDL 은 프레임으로 반올림하므로 최대 반 프레임(29.97p = 17ms)
> 밀려 발화 시작에 걸린다. 양자화를 맞추기 전엔 마지막 패스가 13곳만 잡았는데,
> 맞춘 뒤 58곳을 잡았다.

**실측 (260729 Q&A 3편, 같은 전사 캐시로 비교)** — 접합부 음량을 정리 오디오에서 직접 측정:

| | 변경 전 | 변경 후 |
|---|---|---|
| 컷 | 285개 | **207개** |
| 러닝타임 | 26:14 | **27:50** (+1:36) |
| 접합부 음량 중앙값 | -31.4dB | **-35.9dB** |
| p90 | -12.1dB | **-33.4dB** |
| 최대(가장 시끄러운 접합부) | **-4.1dB** | **-32.5dB** |
| 기준(-33dB) 초과 | 311/568 (54.8%) | **3/412 (0.7%)** |

원본의 12.8% 를 지우던 것이 7.5% 로 줄었다 — **의도한 트레이드오프다.** 제1원칙이
'자연스러움 > 최대 제거' 이고, 말 한복판에서 끊던 접합부(최대 -4.1dB = 또렷한 발화)가
전부 사라졌다. 더 타이트하게 가고 싶으면 `CUT_GATE_MARGIN` 을 올린다(느슨해짐).

> **`output/<base>_words.json` 을 절대 지우지 말 것.** 전사 캐시다.
> 지우면 재전사가 일어나는데 faster-whisper(GPU float16)는 비결정적이라 결과가 달라지고,
> 그 차이가 말더듬·숨소리 판정으로 증폭돼 **컷 개수가 크게 바뀐다** (실측 210컷/26:45 ↔ 285컷/26:14).

### 3) 자막 — 반드시 이 순서

```bash
python engine/apply_glossary.py "output/<base>_cut.srt" --profile 부동산롱폼   # ① 교정
python engine/subtitle_polish.py "output/<base>_cut.srt"                      # ② 25자 분할
```

**순서를 바꾸면 안 된다.** 분할 후에 교정하면 줄 나눔이 달라져 문맥 규칙이 안 맞는다.
분할본을 되돌리려다 `.srt.bak` 을 잘못 복원해 교정을 통째로 날린 적이 있다 —
교정 완료본은 `output/_keep/` 에 따로 복사해 두는 게 안전하다.

- 한 줄 **25자**, 2줄 금지 (비블 자막 = 한 줄 + 검은 박스. 25자가 화면 폭의 약 2/3)
  - 20으로 낮췄다가 **되돌렸다** — 화면에서 짧다는 피드백. 아래 학습 절 참고.
- **온점(.)·쉼표(,)는 넣지 않는다.** `? !` 와 `... …`(말 끄는 표현), 숫자 안의 점·쉼표
  (`1.2%`, `8,500`)는 남긴다. `SUB_STRIP_PUNCT="all"`. 앞 단계들이 문장부호로 줄 경계를
  판단하므로 **맨 마지막에** 적용된다.

### [중요] 자막 싱크 — 두 곳에서 어긋났었다

1. **비례 분할 금지.** 25자 제한은 `regroup(max_chars=)` 로 **처음부터** 지킨다.
   이미 만든 자막을 `subtitle_polish` 가 쪼개면 시간을 글자수 비례로 나눠 실제 발화와 어긋난다(실측 최대 850ms).
   분할이 0건이어야 정상 — 로그의 `633→633` 처럼 개수가 안 늘어야 한다.
2. **타임라인 양자화.** `build_mapper(keeps, fps=...)` 로 영상 XML과 **같은 프레임 단위**로 누적해야 한다.
   실수로 누적하면 클립마다 ±1프레임씩 어긋나 285컷에 걸쳐 쌓인다 (실측 평균 204ms · 최대 349ms, 뒤로 갈수록 심함).

### 4) 오디오

```bash
python engine/make_cut_audio.py "cam01/cam01_TC0.MP4"
```

`_cut_audio_flat.wav` (컷 적용된 단일 파일)를 A1 의 `00:00:00:00` 에 올린다.

> **오디오를 EDL 이벤트로 쪼개지 말 것.** 한 이벤트에 릴 2개(비디오+오디오)를 쓰면
> 프리미어가 오디오 소스 in점을 무시하고 **매 컷마다 0부터 재생**한다.
> 그리고 플랫 오디오는 **샘플 단위로 잘라야** 한다 — ffmpeg concat 디먹서의 inpoint/outpoint 는
> 구간마다 밀려서 285구간에 12.6초나 초과했다. 지금 구현은 오차 0.1ms.

### 5) EDL

```bash
python engine/make_edl.py "cam01/cam01_TC0.MP4" --cam2 "cam02/cam02_TC0.MP4" <오프셋초>
```

> **EDL 타임코드는 30프레임까지다.** 59.94p 이상 소스를 60프레임 TC(`00:00:20:58`)로 쓰면
> 프리미어가 프레임 필드 30 이상을 못 읽고 클립을 엉뚱한 자리에 놓는다 — 증상은
> **타임라인 중간중간 빈 공백 + 오디오 싱크 어긋남**. 엔진이 30fps 초과 소스는
> **소스 2프레임 = TC 1프레임**으로 접어 29.97 DF 로 쓴다(컷 지점은 33ms 격자에 스냅).
> 이 코너는 29.97p 라 그대로지만, 카메라를 60p 로 바꾸면 바로 걸리는 부분이다.
>
> **fps 를 상수로 박지 말 것.** `make_edl`·`make_cut_audio`·`verify_episode` 에 29.97 이
> 상수로 있었다. `_cut.xml` 의 프레임 번호는 **소스 실제 fps 단위**라, 59.94p 회차에서
> 컷이 2배로 어긋나고 오디오가 절반 길이로 잘렸다. 지금은 `probe_media` 로 받아 쓴다.
>
> **`* FROM CLIP NAME` 은 모든 이벤트가 같아야 한다.** 프리미어는 이 값으로 미디어 항목을
> 구분한다. 값이 다르면 이벤트 수만큼 별개 오프라인 클립이 생겨 **미디어 연결을 클립 수만큼
> 손으로** 해야 한다(실측 93클립 = 93번). 이름은 실제 연결할 파일명(`_TC0.MP4`)과 같게 둔다 —
> 그래야 '미디어 연결'에서 프리미어가 자동으로 찾는다.
>
> **멀티캠은 컷을 절대 빼지 말 것.** 범위를 벗어나는 컷(그 캠이 아직/이미 녹화 전후)은
> 제외하지 말고 소스만 clamp 한다. 컷 개수·레코드 TC 가 캠마다 같아야
> 레이어를 겹쳐 교차편집(클립 활성/비활성)을 할 수 있다.
> 제외했더니 208 vs 285 가 되면서 시퀀스 시작 TC까지 어긋났다.

### 6) 프리미어 조립

1. 각 `.edl` 가져오기 → 오프라인 릴에 **미디어 연결**
   - `CAM01` → `cam01_TC0.MP4` / `CAM02` → `cam02_TC0.MP4`
2. cam02 시퀀스 전체 복사 → cam01 시퀀스 **V2** 에 `00:00:00:00` 기준 붙여넣기
3. `_cut_audio_flat.wav` → **A1** 의 `00:00:00:00`
4. `_cut.srt` 가져오기
5. 타임라인 전체 선택 → **Ctrl+Shift+D** (컷 클릭음 제거)

교차편집은 V2 클립을 구간별로 활성/비활성해서 쓴다.

---

### 7) 카테고리 분류 + 축약본 (선택)

긴 회차를 짧게 줄이거나 내용을 카테고리로 나눠 보고 싶을 때.

```bash
python engine/make_highlight.py "<원본.MP4>" --segments output/<base>_segments.json
```

구간 JSON 은 **컷 타임라인(=`_cut.srt` 와 같은 기준)** 의 `M:SS` 로 쓴다.
`keep:false` 인 구간은 축약본에서 빠진다(제작 지시·잡담·중복).

산출물: `_final.edl`(축약본) · `_highlight_audio.wav` · `_highlight.srt` ·
`_full_labeled.edl`(전체) · `_categories.md`(구간 근거 + 조립 순서).

> **카테고리를 트랙으로 나누지 말 것.** 카테고리마다 EDL 을 뽑아 V2·V3… 에 쌓아 봤는데
> 못 쓴다 — 카테고리 경계가 컷 중간을 갈라 104컷이 129조각이 되고, 각 EDL 이 자기 구간만
> 갖고 나머지는 비어 있어 조립이 안 된다. **시퀀스 하나**에 담고 컷은 쪼개지 않는다.
> (카테고리를 클립 이름에 넣는 것도 안 된다 — 위 `FROM CLIP NAME` 항목 참고)
>
> **오디오·자막은 EDL 이 선언한 클립 길이에서 만든다.** 구간 단위로 따로 자르면 스냅·반올림이
> 어긋나 A/V 싱크가 밀린다(실측 5.0ms → 0.1ms). 자막은 클립 경계에서 토막나므로
> (469줄 중 80줄이 76ms 조각) 같은 문장을 다시 병합한다.

### 시퀀스 설정 · 들어갔는지 확인

시퀀스는 **소스와 같은 프레임레이트**로 만든다. 타임베이스는 **생성 후에 못 바꾸므로**
틀렸으면 시퀀스를 지우고 다시 만들어야 한다.

가져온 직후 **마지막 클립이 끝나는 타임코드**가 `make_edl`/`make_highlight` 가 찍어준
시퀀스 길이와 같은지 본다. **2배로 나오면 타임베이스가 절반으로 잡힌 것**이다.

---

## 성능 — 4K HEVC 는 무겁다

컷 285개 × 2트랙 = 클립 570개에 소스가 롱GOP 4K라 스크럽이 매우 느리다. 대응:

- 재생 해상도 1/4
- 환경설정 > 미디어 > **하드웨어 가속 디코딩** 켜기
- 프로젝트 설정 > 렌더러 **Mercury Playback Engine GPU 가속(CUDA)**
- 1080p 시퀀스 + "프레임 크기로 조정"
- 그래도 무거우면 **프록시**(720p 올인트라)를 만들어 붙인다

---

## 이 코너의 편집 방향

- **자연스러움 > 최대 제거.** 반복이 개그다 — "빨리빨리 찍어봐", "10억 10억", "떴어 떴어",
  "본진의 1등을 잡아라" 반복은 살린다. 엔진이 '더듬/중복'으로 지우면 되살릴 것.
- 고유명사 오인식은 채널 신뢰도에 직결한다. 특히 **송도/성도**, **병점역 아이파크캐슬**,
  **신생아 특례대출** — `glossary.txt` 로 매회 잡는다.
- 실제로 말하지 않은 **브랜드명 환청**(예: BHC)은 반드시 제거한다.
- 판단이 갈리는 표현(예: 수위 있는 농담)은 임의로 고치지 말고 편집자에게 물어본다.

## [학습] 비블 완성본과 대조한 결과 (2026-07-30)

비블이 260729 Q&A 3편을 **컷편집·자막·색보정까지 마친 시퀀스**(`대표님미빅님 Q&A3편(최종본)`,
2캠 교차편집 전)와 엔진 산출물을 비교했다. 아래 숫자는 전부 실측이다.

**측정은 프리미어에서 직접 읽는다 — EDL 재내보내기 필요 없다.**

```bash
python engine/mcp_cut_diff.py "대표님미빅님 Q&A3편(최종본)" \
  "output/_prev_260728_부동산/<base>_cut.xml" "output/_v207_260730/<base>_cut.xml"
```

프리미어가 열려 있으면 MCP(`engine/premiere_mcp.py`)로 클립별 소스 in/out 을 초 단위로 읽는다.
**여러 버전을 함께 넘길 것** — 납품본 기준으로만 보면 그 뒤 이미 고친 것까지 두 번 보정한다.

### 1) 엔진이 담당하는 몫은 컷 작업의 1/4 정도다

| | 컷 | 유지 | 원본대비 |
|---|---|---|---|
| 원본 | — | 30:06 | 100% |
| **비블 완성본** | **174** | **13:50** | **46.0%** |
| 엔진 285컷(납품) | 285 | 26:14 | 87.2% |
| 엔진 207컷(현재) | 207 | 27:50 | 92.5% |

비블은 16:16 을 지웠고 엔진은 3:52(285컷) / 2:16(207컷) 을 지웠다. 차이 12~14분은
전부 **내용 컷**이다 — 납품본 285컷 중 **134컷(9:35)이 통째로 삭제**됐고, 40컷은 일부만,
111컷이 거의 그대로 쓰였다.

→ **러프컷을 '거의 완성'으로 취급하지 말 것.** 엔진 출력은 내용 컷의 출발점이다.
길이를 절반으로 줄이는 판단(무엇을 버릴지)은 여전히 사람 몫이고, 자동화하려면
`content-research` 로 내용 컷 후보를 먼저 뽑아 붙이는 게 순서다.

#### 내용 컷 기준 (프로젝트 폴더 `삭제컷.txt` 에서 — 26:14 → 18:43 → 13:11 계획)

비블이 실제로 채택한 삭제 사유다. `content-research` 가 구간을 제안할 때 이 분류를 쓴다.

| 버리는 것 | 실제 사례 |
|---|---|
| 주제에서 벗어난 지역 썰 | 수도권 사연 중 **청주 지웰시티 지방 아파트 썰 통삭제(-1:47, 최대 덩어리)** |
| 앞서 다룬 내용 재확인 | 아웃트로에서 송도/오산 차트 다시 보기(-29초) |
| 늘어지는 리액션 | 주식/대출 한탄(-22초) |
| 본론과 무관한 사담 | 결혼·나이·전세 잡담(-37초), 인천 vs 송도 주민 인식(-12초), 중계동 학폭/모텔 비유(-16초) |
| 스튜디오 상황 농담 | 마우스 클릭질 답답함·사투리(-32초), 전화번호 공개·구인 농담(-21초), 치킨 드립(-15초) |
| 나열형 TMI | 남동탄 단지 이름 나열(-31초) |
| 자화자찬 | "수업할 때 친절하게 알려준다"(-10초) |

**남기는 것**: 챕터 브릿지 멘트("먼저 첫 번째 사연입니다"), 사연 본문, 차트 비교,
대출·매수 솔루션, 핵심 결론.

> 컷의 **90%가 잡담·사담·농담**이다. 그런데 이 코너의 제1원칙은 '자연스러움 > 최대 제거'이고
> 반복 개그("빨리빨리 찍어봐", "떴어 떴어")는 살리라고 돼 있다 — **모순이 아니다.**
> 살리는 건 본론 위에 얹힌 짧은 말맛이고, 버리는 건 본론을 멈추고 옆길로 새는 덩어리다.
> 판단 기준은 길이와 위치지 '농담이냐'가 아니다.

### 2) [중요] 과잉 제거는 없었다 — 3.1초

`BASELINE.md` 의 가정은 "비블 수정 상당수는 말 끝나기 전 끊김(과잉 제거)에 대한 것"이었다.
**검증 결과 틀렸다.** 우리가 버린 구간 중 비블이 되살린 총량:

| 기준 | 우리가 버린 양 | 비블이 되살린 양 |
|---|---|---|
| 285컷 | 3:52 | **3.1초** (10조각, 최대 0.73초) |
| 207컷 | 2:16 | **0.83초** |

즉 컷 경계 무음 가드로 제거율을 12.8% → 7.5% 로 낮춰 얻은 것은 **2.3초**고,
그렇게 남긴 2.3분은 비블이 결국 다 잘라냈다. 가드 자체는 접합부 '말 중' 비율을
54.8% → 0.7% 로 고친 실효가 있으니 **유지하되, 제거량은 되찾는 방향**
(`CUT_GATE_MARGIN` 을 올려 느슨하게)이 맞다.

> **되살림 지표는 컷 '분량'만 본다.** 같은 분량을 남기면서 컷 '위치'가 나쁜 경우
> (음절 중간 접합)는 이 숫자에 안 나타난다 — 그건 접합부 RMS 로 따로 본다.

### 3) 컷 경계는 그대로 채택됐다 — 패딩은 손대지 말 것

유지된 컷의 머리(in)/꼬리(out) 델타 (+ = 비블이 더 잘라냄):

| 기준 | 머리 중앙 | 꼬리 중앙 | \|Δ\|>0.3s |
|---|---|---|---|
| 285컷 (n=136) | **+0.000s** | **+0.000s** | 머리 20.6% / 꼬리 12.5% |
| 207컷 (n=89) | +0.033s | +0.067s | 머리 22.5% / 꼬리 15.7% |

중앙값 0 = **비블이 우리 컷 경계를 거의 그대로 썼다.** 평균이 +0.38s 인 건 소수의 큰 값
(문장 앞부분을 통째로 버리는 내용 트림) 때문이고, p90 은 머리 +1.07s / 꼬리 +0.40s 다.
`PAD_LEAD 0.30` / `PAD_TAIL 0.12` 는 지금 값으로 충분하다.
207컷 쪽 델타가 더 크다는 것도 같은 결론을 가리킨다 — 285컷 경계가 비블 취향에 더 가까웠다.

### 4) 산출물 형식은 맞았다

- `_cut_audio_flat.wav` 를 **A1 에 올려 66조각으로 쪼개** 쓰고 있다 → 단일 플랫 WAV 방식이 맞다.
- 미디어는 `_TC0` 사본이 아니라 **원본 MP4** 로 연결해 뒀다(임포트 후 갈아끼운 것).
- 2캠 교차편집은 아직 안 한 상태다(V1 176클립 / V2 174클립, V1 뒤쪽 타임라인에
  원본 전체 클립 2개를 참고용으로 파킹).

### 비교할 때 빠지는 함정

- **캠을 맞춰서 볼 것.** cam01 `_cut.xml` 을 수정본의 cam02 트랙과 비교하면 경계델타
  중앙값이 ∓1.03s 로 나온다 — 그건 오차가 아니라 **cam02 싱크 지연값**(1.0377s)이다.
  `mcp_cut_diff.py` 는 XML 의 소스 파일명으로 트랙을 고른다.
- 편집 트랙에 **참고용 원본 전체 클립**(30분)이 섞여 있다. 길이로 걸러야 컷 통계가 안 망가진다.

### 5) 자막 — 비블이 실제로 고친 것

```bash
python engine/sub_diff.py "<프로젝트.prproj>" "output/<base>_cut.srt"
```

> **캡션은 프리미어 API 로 못 읽는다.** MCP `read_sequence_captions` 는 캡션 트랙이
> 멀쩡히 있어도 항상 `trackCount:0` 을 돌려준다(Adobe 제한 — `createCaptionTrack` 은
> 쓰기만 있고 읽는 짝이 없다). `engine/prproj_captions.py` 가 `.prproj`(gzip XML)를
> 직접 파서 `<Caption>` 의 틱 타이밍 + base64 FlatBuffer 안의 UTF-8 본문을 꺼낸다.

우리 633줄 ↔ 비블 757줄, 문장부호·공백 무시 **일치율 99.90%** — 우리 SRT 를 손본 게 맞다.

#### ① [철회] 온점·쉼표 규칙은 원래대로 — 지우는 게 맞다

여기 한때 "비블은 온점·쉼표를 되살리므로 지우면 안 된다"고 써 뒀었다. **틀렸다. 철회한다.**

| | 온점 | 쉼표 | 물음표 | 말줄임 |
|---|---|---|---|---|
| 우리 납품본 | 2 | 0 | 68 | 13 |
| 문제의 캡션 | 225 | 65 | 68 | 13 |

**근거로 삼은 캡션이 최종본이 아니었다.** 그 캡션은 **26.23분** 타임라인에 얹혀 있는데
그건 우리 285컷 납품본 길이(26.24분)다 — 비블의 최종 편집본은 **13.84분**이다.
즉 납품 직후 **중간 상태**를 완성본으로 착각했다. 프로젝트 폴더의 프리미어 텍스트 패널
전사본(`대표님미빅님 Q&A3편.txt`, 발표자 1/2 · 문장부호 가득)을 보면 그 단계에서
문장부호가 섞여 든 것으로 보인다.

> **캡션을 근거로 쓸 때는 그게 얹힌 시퀀스 길이가 최종본과 맞는지 먼저 확인할 것.**
> 텍스트가 99.90% 일치한다고 해서 그 트랙이 최종 상태인 건 아니다.

→ `SUB_STRIP_PUNCT="all"` 유지. 원래 규칙이 맞았다.

#### ② [철회] 25자 그대로 간다

| | 중앙 | 평균 | ~10자 | 11~15 | 16~20 | 21~25 |
|---|---|---|---|---|---|---|
| 우리 633줄 (25자) | 19자 | 18.3 | 4.7% | 14.8% | 49.9% | 30.5% |
| 문제의 캡션 757줄 | 15자 | 15.5 | 10.4% | 42.7% | 33.3% | 13.6% |
| 260630 테스트 (20자) | 14자 | 13.6 | 13.6% | 57.5% | 28.9% | 0% |

캡션이 더 잘게 끊겨 있길래 "25자는 상한이지 목표가 아니다"라고 보고 `SUB_MAX_CHARS` 를
20으로 낮췄는데, **260630 회차로 뽑아 보니 화면에서 짧다**는 피드백을 받아 25로 되돌렸다.

**①과 같은 뿌리의 오류다.** 문장부호도 길이도 전부 그 26.23분 캡션에서 나온 결론이었고,
그건 최종본이 아니라 납품 직후 중간 상태였다. **둘 다 철회한다.**

> 남는 사실 하나: 자막 길이는 `subtitle_polish` 가 아니라 상류
> `make_subtitles.regroup(max_chars=)` 에서 정해진다. `subtitle_polish` 는 상한을 넘는 줄을
> 쪼갤 뿐 짧은 줄을 다시 합치지 않는다 — **길이를 바꾸려면 컷 단계를 다시 돌려야 한다**
> (전사 캐시가 있으면 재전사는 없다).

#### ③ 줄 경계 자체는 대체로 옳았다

우리 줄 끝 어절이 비블 자막에서 다음 줄 머리로 넘어간 건 **5건/633줄(0.8%)**.
즉 비블의 작업은 '경계를 옮긴 것'이 아니라 **기존 경계를 지키며 더 쪼갠 것**이다.
어미 사전 기반 분할(`make_subtitles.end_score`)은 방향이 맞다.

#### ④ 단독 리액션은 본 자막에서 뺀다

우리 자막의 한 줄짜리 `네` 4개가 비블 최종본엔 없다. **C1·C2 캡션 트랙은 "아~", "진짜~?"
같은 예능형 재미 자막을 띄우려고 레이어로 분리해 둔 것**이라(비블 확인), 리액션·감탄사는
본 자막 트랙에 있으면 안 된다. 엔진이 그 레이어를 만들 필요는 없지만, **본 자막에서
단독 감탄사 줄은 빼야** 한다.

#### ⑤ 오타·오인식 13곳 → `glossary.txt` 반영 완료

`송도리스 더스카이`→`송도 더스카이`, `모아미래도 세교대시야`→`모아미래도 세교`,
`갱상도`→`경상도`, `GTX-C`→`GTX C`, 숫자 누락(`5억 1.2`→`15억, 1.2%`) 등.

> **사투리 어감은 표준어로 펴지 말 것.** 비블은 `아닙니까` 를 **`아닙니꺼`로 되살렸다**
> (`갱상도`는 오타라 고치면서도). 이 코너의 말맛에 해당하므로 교정 대상이 아니다.
> 문맥이 필요한 교정은 단어가 아니라 **구 단위**로 넣는다.
>
> 비블도 놓친 오타가 있다(`송도 더샤 퍼스트파크`). 최종본에 남아 있다고 다 정답은 아니다 —
> 명백한 단지명 오타는 우리가 잡아 주는 게 맞다.

### 6) 색보정

#### [제1원칙] 색을 입히지 않는다 — 지배적인 캐스트를 걷어내 중성으로 되돌린다

비블의 정의 그대로다:

> 붉은기가 강하면 붉은 기를 걷어내고, 녹색기가 강하면 녹색 기를 걷어내고,
> 푸른기가 강하면 푸른 기를 걷어낸다. **그게 네츄럴한 색온도다.**

**그래서 보정 방향은 고정값이 아니라 소스마다 달라진다.** 260729 회차가 주황(+35°)인 건
**그 회차 조명이 푸르렀기 때문**이지(원본 R−B −18.1) 이 코너의 고정 룩이 아니다.
다음 회차가 붉게 찍혔으면 **반대로 시안 쪽으로** 밀어야 한다.

| 소스의 캐스트 | 미는 방향 | 휠 각도(0°=빨강, 양수=노랑쪽) |
|---|---|---|
| 푸른기 (B 높음) | 주황/웜 | 약 +20~40° ← 260729 회차가 이 경우 |
| 붉은기 (R 높음) | 시안 | 약 +180° |
| 녹색기 (G 높음) | 마젠타 | 약 −60~−90° (300° 부근) |

> **값을 복사하지 말고 캐스트를 재서 방향을 정할 것.** 이건 자동화된다 —
> `engine/apply_grade_still.py` 의 `whitebalance()` 가 그 구현이다.
>
> **기준은 전체 평균이 아니라 '원래 중성인 면'(벽·천장)이다.** grey-world 로 전체 평균을
> 맞추면 나무 책상·피부·초록 셔츠처럼 **원래 유채색인 것까지** 중성으로 끌려가 화면이 죽는다.
> `neutral_mask()` 로 **밝고(상위 40%) 채도 낮은(<0.12)** 픽셀만 골라 그 R·G·B 평균이
> 같아지도록 채널 게인을 건다.
>
> 실측(260729 스틸) — 중성면 채널 편차:
>
> | | 중성면 R / G / B | 편차 |
> |---|---|---|
> | 원본 | 143.8 / 152.6 / 159.4 | **15.55** (강한 푸른기) |
> | 휠 틴트만 | 188.2 / 191.7 / 194.3 | 6.14 |
> | **+ 중성면 WB** | 191.2 / 192.2 / 192.8 | **1.63** |
>
> 보정 후 전체 평균은 R157.9 G154.0 B153.6 으로 R 이 조금 높은데, **이건 캐스트가 아니라
> 나무 책상과 피부다.** 중성이어야 할 면이 중성이면 된 것이지 화면 전체를 회색으로
> 맞추는 게 아니다 — 판단은 **중성면 편차**로 한다.

**그리고 색은 조금만 얹는다.** 세 휠의 채도가 0.10~0.19 로 크로스헤어가 중심에서 겨우 벗어난
정도이고, **세 휠 각도를 같은 방향으로 통일**해 톤 영역별로 색이 갈리지 않게 한다.
전체 채도는 오히려 **낮춘다**(×0.783). **더하는 보정이 아니다.**

#### Lumetri 값은 읽을 수 있다

```bash
python engine/read_grade.py          # 활성 시퀀스의 클립별 Lumetri → 다른 설정만 묶어 출력
```

> **`list_clip_effects`(MCP)는 쓰지 말 것.** `trackIndex`/`clipIndex` 를 무시하고 **항상 첫
> 클립만** 돌려준다(실측 — 350클립이 전부 같은 값·같은 inPoint 로 나왔다). `execute_extendscript`
> 로 직접 순회해야 한다. 그리고 ExtendScript 는 **마지막 식이 아니라 IIFE 의 return** 만 받는다
> (`1+1` → `undefined`, `(function(){return 1+1;})()` → `2`).

#### 비블이 실제로 넣은 값 (260729 Q&A 3편, 클립 350개 전수)

> **보정한 건 cam01 뿐이다.** cam02(V1)는 색보정을 안 한 원본 상태다(비블 확인).
> 그래서 **cam02 값이 곧 미보정 기준선**이고, 두 값의 차이가 보정 내용 그 자체다.

| | cam02 = **미보정 기준선** | **cam01 = 보정본** | 차이 |
|---|---|---|---|
| 기본 교정 전부 (온도·색조·노출·대비·밝은영역·어두운영역·흰색·검정) | 0 | **0** | **안 건드림** |
| 기본 채도 | 100 | 100 | 안 건드림 |
| 크리에이티브 채도 | 163.23 | **127.74** | **×0.783 — 낮췄다** (UI 환산 약 78) |
| LUT · 룩 · 곡선 · 비네팅 · Auto Tone | 없음/0 | 없음/0 | 안 씀 |

**색상 휠 (cam01) — 인덱스 79 블롭을 푼 값**

| 휠 | 각도 | 루마 | 채도 |
|---|---|---|---|
| 어두운 영역 | 35.29° | **0.032** (맨 아래) | 0.115 |
| 미드톤 | 21.72° | **0.846** (위) | 0.191 |
| 밝은 영역 | 20.65° | **0.940** (위) | 0.100 |

**핵심 네 가지.**

1. **대비는 휠의 루마로 준다.** 어두운 영역을 바닥까지 내리고 미드톤·밝은 영역을 올렸다.
2. **색은 아주 살짝만 얹는다.** 세 휠의 채도가 0.10~0.19 로 크로스헤어가 중심에서 겨우
   벗어난 정도다. 그리고 **세 휠의 각도가 20~35° 로 전부 같은 방향**이다 —
   톤 영역별로 색이 갈리지 않게 통일했다. 이게 "네츄럴"의 실체다.
3. **전체 채도는 낮춘다**(×0.783). 카메라가 과채도로 잡은 걸 눈에 가깝게 **내리는** 방향이고,
   LUT 도 룩도 곡선도 안 쓴다. **더하는 보정이 아니다.**
4. **온도 슬라이더는 0이다.** 색온도를 기본 교정으로 잡지 않는다 — 기본 교정 탭은 아예 안 연다.
   그리고 **클립별 수작업이 아니라 캠 전체에 보정 1개**다(174클립이 값 1가지).

> **블롭 해석은 패널 스크린샷과 대조해 확인했다.** 처음엔 `(각도, 채도, 밝기)` 로 읽었는데
> 틀렸다 — 휠 크로스헤어가 거의 중앙인데 두 번째 값이 0.85·0.94 로 커서 앞뒤가 안 맞았다.
> **가운데가 루마**이고, 패널의 세로 슬라이더 위치(맨아래/위/위)와 0.032/0.846/0.940 이 정확히 맞는다.
>
> **각도는 0° = 빨강, 양수 = 노랑 쪽(주황/웜)이다.** 스틸에 주황(+35.3°)과 핑크(−35.3°)를
> 각각 먹여 비블에게 고르게 해 확정했다 — **주황을 골랐다**. RGB 방향으로는
> `R +0.66 / G +0.08 / B −0.75` 라 **파란 기를 빼고 붉은 기를 올린다**.
> 원본이 R−B −18.1 로 상당히 푸른데(스튜디오 조명), 보정 후 +3.2 로 중성을 살짝 넘긴다.
> 이게 "사람 눈에 가까운 네츄럴한 색온도"의 정체다.
>
> **저장값은 UI 눈금이 아니다.** 미보정 클립의 크리에이티브 채도가 163.23 으로 읽히는데
> UI 기본값은 100 이다. **절대값을 UI 숫자로 읽지 말고 기준선 대비 비율로 볼 것**
> (선형이면 UI 환산 약 78, 미확인).
>
> 미보정인 cam02 에도 Lumetri 이펙트 자체는 붙어 있다(참고용 원본 클립 2개에만 없다).
> 이펙트 유무로 보정 여부를 판단하면 안 되고, **기준선과 값을 비교**해야 한다.
>
> **아직 확정 못 한 것: 휠 루마 0~1 이 실제로 몇 스톱인가.** 재현할 때 자유 변수로 남는다
> (스틸 테스트에서는 중립 0.5 · 스케일 0.18 로 썼고 비블이 그 결과를 골랐다). 다만 그 값이면
> 미드·하이라이트가 올라가 전체가 밝아지는데, 읽어낸 어두운 영역 0.032(맨 아래)대로면
> 그림자가 눌려 대비가 살아야 한다 — **3구간 가중치 모델이 Lumetri 실제 동작과 다를 수 있다.**
> 프리미어에서 보정본 프레임을 `export_frame` 으로 뽑아 원본과 픽셀 비교하면 역산해 확정된다.
>
> **확인 범위**: 130개 파라미터 중 각 그룹을 훑는 31개를 비교했고 다른 것은 위 둘뿐이었다.
> 곡선/HSL 영역(57~71)은 브리지가 응답을 멈춰 대조하지 못했다 — 다만 곡선 활성 플래그와
> RGB 곡선 블롭(52~56)은 양쪽이 같았다.
>
> **브리지가 느리다.** 파라미터를 100개 넘게 도는 스크립트는 타임아웃 난다. 인덱스를
> 30개 이하로 끊고, 비교는 ExtendScript 안에서 끝내 결과만 받아올 것.

#### [기준 2] 260630 반도체 초호황 — 비블이 직접 잡은 값 (2026-07-31)

내가 넣은 값을 보고 **"어둡고 칙칙하다, 내가 잡은 건 더 밝고 화사하고 컬러풀하다"**는
피드백을 받았다. 비블이 첫 컷을 직접 보정해 줘서 그 값을 읽었다.

| Lumetri | 값 |
|---|---|
| 기본 교정 | 온도 −4 · 색조 +8 · 흰색 +12 · 검정 +4 (나머지 0) |
| **크리에이티브 채도** | **150.1** |
| 색상 휠 어두운 영역 | 각도 **5.57°** · 루마 **0.180** · 채도 0.059 |
| 색상 휠 미드톤 | 각도 **233.13°** · 루마 **0.936** · 채도 0.061 |
| 색상 휠 밝은 영역 | 각도 **268.18°** · 루마 **0.999** · 채도 **0.225** |

> **'루마' = 휠 옆의 세로 막대바다**(패널에서 휠 왼쪽에 붙어 있는 슬라이더).
> 0.5 가 중앙이고, 위 값은 어두운영역 = 중앙 아래 / 미드톤 = 위쪽 / 밝은영역 = 거의 최상단.
> 비블은 **휠(색)과 막대바(밝기)를 같이** 움직인다 — 밝기 조정이 이 안에 들어 있다.
> 블롭 하나에 각도·루마·채도가 같이 담겨 있어서 **블롭을 통째로 복사하면 밝기도 따라간다**
> (인덱스 79). 따로 옮길 필요 없다.

**"밝고 화사하고 컬러풀"의 정체는 셋이다.**

1. **미드·하이라이트 루마를 거의 끝까지 올린다**(0.936 / 0.999). 이게 '밝고'.
   내가 넣은 값에는 휠이 아예 비어 있어서 어두웠다.
2. **스플릿 토닝** — 섀도우는 빨강 쪽(5.57°), 미드·하이라이트는 **파랑 쪽**(233°·268°).
   특히 하이라이트 채도 0.225 로 셋 중 가장 세다. 이게 '화사한'이다.
3. **크리에이티브 채도 150.1.** 이게 '컬러풀'.

> **[정정] "세 휠 각도를 같은 방향으로 통일한다"는 규칙이 아니었다.**
> 260729 에서 셋 다 20~35°(주황)였길래 그렇게 적었는데, 이 회차는 **섀도우 5° / 미드 233° /
> 하이 268°** 로 갈라진다. 휠 방향은 회차마다 다르다 — 제1원칙 그대로다.
>
> **섀도우 루마도 0.032(260729)와 0.180(260630)으로 다르다.** 스틸 레시피의 `shadow_luma 0.50`
> 은 260729 값을 되살리려고 내가 고른 숫자지, 비블이 쓰는 값이 아니다.

#### [납품 규칙] 색보정은 **맨 앞 1컷만** 넣는다 (2026-07-31 비블 지시)

> **"컷편집을 요청하면 맨 앞 1컷만 색보정을 해서 컷편집된 시퀀스와 함께 하나의 시퀀스로
> 제공해줘. 내가 맨 앞 색보정 컷을 나머지 컷에 모두 적용할게."**

시퀀스 전체에 넣으면 오래 걸린다(100클립에 수 분). 비블이 첫 컷을 복사해
**특성 붙여넣기(Ctrl+Alt+V)** 로 퍼뜨리는 게 훨씬 빠르다.

```bash
python engine/apply_grade_premiere.py "<시퀀스 이름 일부>"     # 맨 앞 1컷 (기본)
python engine/apply_grade_premiere.py "<시퀀스 이름 일부>" --all  # 전 클립 (느리다)
```

값은 `profiles/budongsan-longfom/grade_lumetri.json` 에서 읽는다 — 위 260630 표를 그대로
저장해 둔 것이고, **색상 휠은 블롭째** 담겨 있어 각도·루마(막대바)·채도가 함께 복원된다.

**컷편집 납품 절차**: `run_episode.py` → 프리미어에서 EDL 가져오기 → 미디어 연결 →
`apply_grade_premiere.py` 로 첫 컷 보정 → 비블이 특성 붙여넣기.

> **매 호출마다 시퀀스를 이름으로 다시 활성화해야 한다.** 브리지 호출 사이에 프리미어가
> 활성 시퀀스를 되돌려서, 지정하지 않으면 엉뚱한 시퀀스(비블 완성본)를 건드린다 —
> 실제로 그럴 뻔했다. `apply_grade_premiere.py` 는 이걸 내장하고 있다.

#### 다음 회차에 쓰는 법

값을 문자열 블롭으로 써넣는 건 위험하니 **프리미어 기능으로 복사**한다.

- 비블에게 캠별로 **Lumetri 프리셋 저장**(효과 컨트롤 > Lumetri 우클릭 > 사전 설정 저장)을
  받아 두면 다음 회차에 클립 전체 선택 후 한 번에 적용된다.
- MCP 로는 `copy_effects_between_clips` / `batch_apply_effect` 로 기준 클립의 보정을
  나머지에 복사할 수 있다.
- **값을 그대로 재사용하지는 말 것.** 위 제1원칙대로 **캐스트를 재서 반대로 미는 것**이라
  방향·양이 회차마다 달라진다. 재사용할 수 있는 건 **방식**(캠당 1개 · 기본 교정은 0 ·
  채도는 낮추고 색상 휠로 중성화 · 세 휠 같은 방향)이지 숫자가 아니다.
- **틴트를 세게 걸면 반대 방향 캐스트가 생긴다.** 휠 틴트 강도 0.35 로 갔더니 붉은기가
  묻어난다는 피드백을 받았다(전체 R−B +3.4). **0.27 로 낮추니 +0.15** 로 사라졌다.
  푸른기를 뺀다고 넘치게 밀면 안 된다 — 목표는 웜이 아니라 **중성**이다.

#### [확정 레시피] — **"색보정 해줘" 하면 이걸로 한다** (2026-07-30 비블 확정)

읽어낸 값에서 출발해 비블 피드백을 **여섯 번** 반영한 최종본이다.
`engine/apply_grade_still.py` 의 `RECIPE` 가 이 값이고, 그게 기본값이다.

```bash
python engine/apply_grade_still.py "<이미지>"           # 확정 레시피
python engine/apply_grade_still.py "<이미지>" --compare # 원본과 나란히
```

| 단계 | 값 | 비블 피드백 |
|---|---|---|
| ① 휠 루마 | 스케일 0.18 · **섀도우 0.50** / 미드 0.846 / 하이 0.940 | 읽어낸 섀도우 0.032 는 **너무 눌린다** → 5단계 중 0.50 선택 |
| ② 휠 틴트 | 주황 +35.3°, **강도 0.27** | 0.35 는 "붉은기가 묻는다" → 낮춤 |
| ③ 중성면 WB | 벽 기준 채널 균등화 | "푸른기 걷어내 중성으로" |
| ④ 흰색(화이트) | **+12** | +6/+12/+20 중 선택. +20 은 5.12% 날아감 |
| ⑤ 크리에이티브 채도 | **UI 98** (읽어낸 78 → +5 → +5 → +10) | 밝아지며 채도가 빠져 보정 |
| ⑥ 블루 틴트 | **+5 포인트** (R×0.99 / B×1.01) | "아주 살짝 블루톤" |
| ⑦ 피부 채도 | **+80** (색상 0~45° · 채도 0.12~0.65 · 명도>0.25) | 5단계 중 선택. +40 은 회차에 따라 원본보다도 낮게 나온다 |

**섀도우 0.032 → 0.50 (2026-07-30 추가)** — 읽어낸 값 그대로 가면 검은 옷·머리카락이
순검정으로 뭉갠다(어두운영역의 **99.3%** 가 <16, 결 σ 2.49 vs 원본 7.70).
**들어올리는 게 아니라 덜 누르는 것**이 답이다 — 사후 토(toe) 리프트는 레벨만 올리고
결을 못 살린다(σ 1.77). 섀도우 루마를 0.50 으로 올리면 σ **9.45** 로 원본을 넘어선다.

**피부 +40 → +80** — 260630 회차에서 +40 은 피부 채도 29.76% 로 **원본(31.63%)보다 낮았다**.
앞단의 전체 채도 ×0.98 을 겨우 상쇄하는 수준이라 피부가 안 산다. 조명이 다른 회차에서는
같은 숫자가 다르게 나온다 — **값이 아니라 방식을 재사용한다**는 원칙이 실제로 드러난 대목.

**③~⑤ 가 중성화, ⑥~⑦ 이 스타일이다. 순서를 바꾸면 안 된다.**
캐스트를 완전히 걷어내 중성을 만든 **다음** 취향을 얹는다.

**⑦은 전체 채도가 아니라 피부만 올린다**(Lumetri 의 HSL 보조에 해당).
전체로 올리면 초록 셔츠·모니터 UI 가 따라 오르고, 무엇보다 **⑥ 블루 틴트가 같이 증폭돼
중성이 밀린다**(실측 R−B −2.51 → −3.35). 피부만 올리면 **−2.15 로 고정**된다.

| | 밝기 | 화면 채도 | 피부 채도 | 중성면 R−B | 날아감 |
|---|---|---|---|---|---|
| 원본 | 130.3 | 17.57% | 28.06% | −15.55 | 0.10% |
| **확정** | 162.1 | 9.89% | **39.01%** | **−2.15** | **0.00%** |

> 피부 채도 39% 는 원본(28%)보다 진하다. 피부 과포화 픽셀(채도>0.55)이 7.0% 로,
> 더 올리면 볼·귀 주변이 붉게 뭉쳐 플라스틱처럼 보이기 시작한다 — **+40 이 상한선에 가깝다.**

> **밝기·틴트를 올린 뒤에는 반드시 소프트 니를 다시 걸 것.** 블루 틴트를 곱하기만 했더니
> 이미 밝던 파랑이 254 를 넘어 **7.17% 가 날아갔다**(니 적용 후 0.00%). 화이트 리프트도
> 마찬가지다 — 니 없이 올리면 벽 상단과 모니터가 순백으로 뭉개진다.
> **날아간 픽셀 비율을 매번 같이 잴 것.**
- **cam02 는 아직 미보정이다.** 2캠 교차편집을 넣으려면 cam02 에도 같은 방향의 보정이 필요하다
  (값은 카메라가 달라 그대로 복사하면 안 맞는다 — 색상 일치로 cam01 에 맞춘다).

### 아직 안 본 것

색상 휠 블롭의 정확한 규격(어느 값이 그림자/중간톤/하이라이트인지)은 확인 못 했다.
비블이 Lumetri 패널에서 실제 슬라이더 위치를 알려주면 대조해 확정할 수 있다.

## 산출물

```
output/
  <base>_cam01_v_tc0.edl        V1 = cam01   ← TC0 사본에 연결
  <base>_cam02_v_tc0.edl        V2 = cam02   ← TC0 사본에 연결 (컷 구조 동일)
  <base>_cut_audio_flat.wav     A1 = 컷 적용 정리 오디오 (-14 LUFS)
  <base>_cut.srt / .vtt / .ass  자막 (한 줄 25자 · 온점/쉼표 없음)
  <base>_words.json             전사 캐시 — 지우지 말 것
  _keep/                        교정 완료본 백업
```

`_v.edl`(실제 TC 기준)도 같이 나오지만 **표준 워크플로는 `_tc0` 쪽**이다.

---

## 회차 기록

| 회차 | 컷 | 러닝타임 | cam02 지연 | 자막 | 비고 |
|---|---|---|---|---|---|
| 260729 Q&A 3편 | 285 | 26:14 (원본 30:06, −12.8%) | 1.0377s (31f) | 633줄 | cam02 앞뒤 2컷 clamp |
| 260729 Q&A 3편 (재작업) | 207 | 27:50 (−7.5%) | 동일 | 643줄 | 컷 경계 무음 가드 적용. 접합부 '말 중' 54.8%→0.7% |
| 260729 Q&A 3편 **(비블 완성본)** | 174 | **13:50 (−54.0%)** | 교차편집 전 | 프리미어 캡션 | 우리 285컷 중 134컷을 내용 컷으로 삭제. 되살린 건 3.1초뿐 (위 학습 절 참고) |
